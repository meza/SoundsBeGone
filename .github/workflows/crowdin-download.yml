name: Crowdin Download

permissions:
  issues: write
  checks: write
  contents: write
  pull-requests: write

on:
  push:
    branches:
      - dev
    paths:
      - "src/main/resources/assets/inventorysorter/lang/en_us.json"
  schedule:
    - cron: '0 */12 * * *' # Every 12 hours
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  crowdin:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: 'dev'

      - name: Synchronize with Crowdin
        uses: crowdin/github-action@8f01d54f70f1713ee3f09d82c2bbb2daeac28689 # v2.17.1
        with:
          upload_sources: false
          upload_translations: false
          download_translations: true
          localization_branch_name: l10n

          create_pull_request: true
          skip_untranslated_strings: true
          commit_message: 'fix(l10n): additional translations'
          pull_request_title: 'fix(l10n): additional translations'
          pull_request_base_branch_name: 'dev'

          pull_request_labels: 'l10n, translation'
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          CROWDIN_PROJECT_ID: ${{ secrets.CROWDIN_PROJECT_ID }}
          CROWDIN_PERSONAL_TOKEN: ${{ secrets.CROWDIN_PERSONAL_TOKEN }}
